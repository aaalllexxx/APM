# Changelog — APM v2.0

## [2.1.0] — 2026-03-17
### Кроссплатформенность
- **Все пути** заменены на `os.path.join()` вместо конкатенации строк (`config.py`, `install.py`, `modules.py`, `remove.py`)
- **`apm.sh`** автоматически использует venv python (`~/.config/apm/venv/bin/python3`) если доступен, иначе fallback на `python3`
- **`setup.sh`** / **`setup.bat`** — исправлен `PROJECT_ROOT` (указывал на `scripts/` вместо корня APM), убран `--break-system-packages`
- **`win2lin.py`** — `System.run()` упрощён (единый `subprocess.run`), добавлены docstrings

### Исправления
- **Обработка ошибок в `apm.py`**: ошибки внутри модулей (например `ModuleNotFoundError` из-за отсутствия зависимости) больше не маскируются как "Команда не опознана" — теперь показывается реальный traceback и имя отсутствующего пакета
- **Deprecated API**: `SourceFileLoader.load_module()` заменён на `importlib.util.spec_from_file_location()` в `apm.py` и `modules/sec.py`
- **`modules/update.py`**: `System.run()` получал список вместо строки — исправлено
- **`modules/sec.py`**: улучшены сообщения об ошибках, добавлен список доступных подкоманд

## [2.0.3] — 2026-03-16
### Улучшения
- **Консолидация ядра**: Фреймворк `AEngineApps` перенесен в корень проекта для обеспечения единого источника кода и предотвращения конфликтов импорта.
- **Оптимизация конфигурации**: Обновлен `pyrightconfig.json` для корректной индексации импортов IDE.

## [2.0.2] — 2026-03-03
### Новые команды
- `apm upgrade` — обновление фреймворка `AEngineApps` внутри текущего проекта (скачивает последнюю версию с GitHub, заменяя файлы ядра).

### Исправления
- В `apm.py` хрупкая проверка входной точки `if "apm" in ...` заменена на стандартную надёжную конструкцию `if __name__ == "__main__":`

## [2.0.0] — 2026-03-03

### Новые команды
- `apm goto` — переход в директорию проекта (keyboard выбор)
- `apm list` — просмотр зарегистрированных проектов с проверкой ✓/✗
- `apm unregister` — удаление проекта из реестра (без удаления файлов)

### Улучшения
- Группированный help (`apm help`) с цветами через rich (Проект/Навигация/Модули/Прочее)
- `apm develop screen` — генерация Screen-класса + HTML-шаблона
- Реестр проектов — `apm create` и `apm init` автоматически регистрируют проект
- Конфигуратор `routers` — выбор между авто и ручной конфигурацией
- `apm config` загружает существующие значения из config.json
- `setup.bat` исключает `__pycache__`, `.git`, `.pyc` при копировании
- `apm.bat` поддерживает `cd` после `apm goto`

### Рефакторинг
- `register_project()` вынесена в `helpers.py` (убрано дублирование из create.py/init.py)
- Проектные модули проверяют наличие `.apm` перед работой
- Кросс-платформенный ввод через `readchar` (вместо `keyboard`)
- Обработка ошибок во всех модулях

### Исправления
- Config: Ctrl+S сохранить, ESC выйти (Windows); s/q (Linux)
